# Base URL for the backend REST API (FastAPI).
# In local dev, this is usually http://localhost:8000
# In production, point to your deployed backend.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
`````

Explanation: Update global styles, add a light modern theme baseline and small utility classes for consistent layout.
````write file="unified-notes-application-336120-336136/notes_app_frontend/src/app/globals.css"
@import "tailwindcss";

:root {
  --bg: #f9fafb;
  --surface: #ffffff;
  --text: #111827;
  --muted: #64748b;

  --primary: #3b82f6;
  --success: #06b6d4;
  --danger: #ef4444;

  --border: rgba(15, 23, 42, 0.10);
  --shadow: 0 10px 24px rgba(15, 23, 42, 0.08);
}

* {
  box-sizing: border-box;
  padding: 0;
  margin: 0;
}

html,
body {
  max-width: 100vw;
  overflow-x: hidden;
  background: var(--bg);
  color: var(--text);
}

body {
  font-family: system-ui, -apple-system, Segoe UI, Roboto, sans-serif;
}

/* Simple reusable classes (keep minimal; Tailwind does most of the work) */
.container-pad {
  padding: 16px;
}

.card {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: 14px;
  box-shadow: var(--shadow);
}

.input {
  width: 100%;
  border: 1px solid var(--border);
  border-radius: 10px;
  padding: 10px 12px;
  background: white;
}

.input:focus {
  outline: 2px solid rgba(59, 130, 246, 0.25);
  border-color: rgba(59, 130, 246, 0.55);
}

.btn {
  border-radius: 10px;
  padding: 10px 12px;
  border: 1px solid var(--border);
  background: white;
  color: var(--text);
}

.btn-primary {
  background: var(--primary);
  color: white;
  border-color: rgba(59, 130, 246, 0.35);
}

.btn-danger {
  background: var(--danger);
  color: white;
  border-color: rgba(239, 68, 68, 0.35);
}

.badge {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  padding: 4px 10px;
  border-radius: 999px;
  border: 1px solid var(--border);
  color: var(--muted);
  background: rgba(15, 23, 42, 0.02);
  font-size: 12px;
}
`````

Explanation: Update app metadata and mount global providers (auth + toasts) for the entire app.
````write file="unified-notes-application-336120-336136/notes_app_frontend/src/app/layout.tsx"
import type { Metadata } from "next";
import "./globals.css";
import { AppProviders } from "@/components/AppProviders";

export const metadata: Metadata = {
  title: "Notes",
  description: "A minimal notes app with tags, search, and settings.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body suppressHydrationWarning>
        <AppProviders>{children}</AppProviders>
      </body>
    </html>
  );
}
